if (WIN32)
	include(CMakeLists-internal-msvc.txt)
else (WIN32)
	if (APPLE)
        include(CMakeLists-internal-macosx.txt)
     else (APPLE)
		include(CMakeLists-internal-unix.txt)
	endif (APPLE)
endif (WIN32)
