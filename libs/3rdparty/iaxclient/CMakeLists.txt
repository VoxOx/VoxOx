if (IAXCLIENT_INTERNAL)
	if (WIN32)
		include(CMakeLists-internal-msvc.txt)
	else (WIN32)
		if (UNIX)
			include(CMakeLists-internal-unix.txt)
		endif (UNIX)
	endif (WIN32)
else (IAXCLIENT_INTERNAL)
	include(CMakeLists-external.txt)
endif (IAXCLIENT_INTERNAL)
