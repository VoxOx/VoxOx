if (WIN32)
	include(CMakeLists-internal.txt)
else (WIN32)
	if (APPLE)
		include(CMakeLists-internal.txt)
	else (APPLE)
		if (SPEEX_INTERNAL)
			include(CMakeLists-internal.txt)
		else (SPEEX_INTERNAL)
			include(CMakeLists-external.txt)
		endif (SPEEX_INTERNAL)
	endif (APPLE)
endif (WIN32)
